name: Maestro CI Pipeline

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  REGISTRY_NAMESPACE: manta-associados
  DOCKER_REGISTRY_PATH: ghcr.io/manta-associados/maestro

# See .github/workflows/maestro-ci.yml for the complete CI pipeline definition
# This is a reference copy for documentation purposes
