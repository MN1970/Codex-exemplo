# Maestro OS v6.0 — Production Dependencies
# Minimal set for runtime (no dev/test tools)
# Note: versions are flexible to allow pip resolver to find compatible sets

# Web framework
fastapi>=0.100
uvicorn[standard]>=0.23
starlette>=0.26

# Core orchestration
schedule>=1.2.0
APScheduler>=3.10
pytz>=2023.3

# HTTP & Networking
requests>=2.30
urllib3>=2.0
httpx>=0.25
aiohttp>=3.8

# Configuration & Validation
python-dotenv>=1.0
pydantic>=2.0
pydantic-settings>=2.0
click>=8.0

# Database & ORM
psycopg2-binary>=2.9
sqlalchemy>=2.0

# Metrics & Monitoring
prometheus-client>=0.19
python-json-logger>=2.0

# Security & Authentication
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
PyJWT>=2.8

# Async & Concurrency
asyncio-contextmanager>=1.0
aiofiles>=23.0

# Lightweight ML (optional, can be commented out)
# numpy>=1.26
# scikit-learn>=1.3
# xgboost>=2.0
